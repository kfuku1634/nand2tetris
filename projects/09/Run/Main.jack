/** Initializes a new Run game and starts running it. */
class Main {
    function void main() {
        var RunGame game;
        let game = RunGame.new();
        do game.run();
        do game.dispose();
        return;
    }
}
